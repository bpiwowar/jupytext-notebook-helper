[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "jupytext-notebook-helper"
authors = [{ name = "Benjamin Piwowarski", email = "benjamin@piwowarski.fr" }]
description = "Build tooling for jupytext-percent teaching notebooks: student/teacher/colab/solution generation, a uv bundle, an index page, and a build that runs every source"
readme = "README.md"
license = { text = "MIT" }
keywords = ["jupyter", "jupytext", "teaching", "notebook"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Education",
]
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = [
    # what a notebook itself needs at run time — the compute-profile ladder and
    # hardware detection — is cs-lab's, not this package's. Depended on here
    # because the author's side reads it: `is_notebook()`, and the note
    # `hardware()` shows, to which this package adds figure visibility.
    "cs-lab>=1.0",
    # rendering a figure in the terminal when a source runs as a script
    # (`make check`), never in a notebook
    "imgcat>=0.5.0",
    "matplotlib>=3.5",
    "pillow>=9.0",
    # build-time filter (jupytext-percent → student/teacher/colab notebooks)
    "jupytext>=1.17",
    "nbformat>=5.10",
    # percent-format headers, read on their own by the practicals manifest
    "pyyaml>=6",
    # tomllib is stdlib on 3.11+; backport for 3.10 (used by uvutils)
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.8",
    "pre-commit>=3.5",
]

[project.urls]
Homepage = "https://github.com/bpiwowar/jupytext-notebook-helper"
Repository = "https://github.com/bpiwowar/jupytext-notebook-helper"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.targets.wheel]
packages = ["jupytext_notebook_helper"]

[tool.ruff]
line-length = 88

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line length handled by the formatter
    "E402",  # module-level import not at top (common in notebooks/filters)
]

[tool.pytest.ini_options]
testpaths = ["tests"]
